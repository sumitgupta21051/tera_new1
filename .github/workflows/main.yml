name: terraform ki practise

on: 
  push: 
    branches: 
      - main
      - feature
  pull_request: 
    branches: 
      - main
      - feature
runs-on: ubuntu-latest

jobs:
  my_first_job:
    name: my_first_job
    runs-on: ubuntu-latest
    steps:
      - name: create file
        run: touch sumit.txt
      - name: data entry
        run: |
          echo "mera name jony h" > sumit.txt
          cat sumit.txt

      
      
