name: terraform practise

on: 
  push: 
    branches: 
      - main
      - feature
  pull_request: 
    branches:
      - main
      - feature

jobs:
  my_first_job:
  name: my first job
  runs-on: ubuntu-latest
  steps:
    - name: create folder
      run: mkdir sumit
    - name: change directory
      run: cd sumit

      

    
    
  
